# u9 — DISCOVERY (P1-D structural-assert suite · inv 1 / 2 / 8 / 12 + a11y)

run 20260803-213143 · TEST + IMPLEMENT attempt 1. **This unit writes no
production code ([u9#c8])** — everything below is reported, not fixed. Written
to `discovery/u9.md` per P1-F; the integrator consolidates into `DISCOVERY.md`.

## A. Spec gaps and contract ambiguities

1. **The worktree carries no contract.** `.claude/super/units/u9.md` and
   `.claude/super/CONSTRAINTS.md` are both absent from the worktree —
   `.claude/super/` holds only `progress.json`. Both were read from the origin
   checkout at `/Users/al03030782/Documents/GitHub/nhn-game-2026/.claude/super/`.
   Worse, `units/u9/design.md` + `spec.md` there are **stale**: they describe a
   portrait sprite-sheet component from the `demos/apothecary` dry-run, not this
   run's structural-assert suite. Same defect u1 and u10 logged — the
   unit-artifact writer reuses a unit dir across runs. This suite follows
   `units/u9.md`; `units/u9/tests.md` is the only artifact this run produced.
   *Fix: stamp `run_id` into the unit dir, or clear it between runs.*

2. **PRD §4 and `units/u9.md` disagree on the fourth invariant.** PRD §4 names
   inv 1, inv 2, inv 10 (no third-party URL in the built bundle) and inv 8 for
   P1-D. `units/u9.md` c4 replaces inv 10 with **inv 12** (seam integrity) and
   never mentions inv 10. The suite follows the unit contract. Nothing is lost —
   inv 10 is already covered by `tests/assets/no-third-party-url.test.ts` (u10) —
   but the PRD and the unit file should be reconciled.

## B. Seam friction · defects found and reported

1. **DEFECT — focus order does not follow visual order at 1280×800. Owner: u3.**
   `e2e/a11y.spec.ts` › `a11y — the windows are tabbed in the order they are laid
   out on the desk`. Tab visits the windows in registry/DOM order
   (`feed · file · store · rep · tally`) while `shell/layout.ts` places them at
   `feed(x14,y94) · rep(x369,y94) · store(x369, below rep) · file(x~880,y94) ·
   tally(bottom band)` — visual order `feed · rep · file · store · tally`. Focus
   therefore jumps left column → right column → middle-bottom → middle-top. PRD
   §4's a11y bar asks for the opposite.
   *Fix (u3): the `#desktop` child insertion order in `shell/boot.ts` /
   `window-registry.ts` should follow `applyLayout()`'s geometry. The taskbar
   order can stay as it is — only the desk's child order matters for Tab.*
   *Why u3's own suite missed it:* `e2e/shell.spec.ts` › `a11y — tab order
   follows the visual order of the chrome` scopes to `#topbar` only.
   **Quarantine (IMPLEMENT):** [u9#c8] forbids this unit from fixing production
   code and [u9#c6] forbids gating u9's slice on another unit's defect, so the
   assert is kept **verbatim** under Playwright's `test.fail()`. The body still
   runs and the failure is still reported; when u3 lands the fix the test turns
   red as "expected to fail but passed", which forces the annotation off. It is
   an expected-failure marker, not a weakened or deleted assert.

2. **DEFECT — `tests/styles/token-lint.test.ts` (u1) is RED on the current
   tree. Owner: u1 or u10.** Its "font-family / font-size outside tokens.css are
   `var()`-driven" assert flags ~520 generated `@font-face { font-family: … }`
   declarations in u10's `src/client/styles/fonts.css`. A `@font-face` block is
   where a face is *declared* — it is what `--myeong` / `--mono` point at, not a
   consumption-site literal — so the lint needs an `@font-face` exemption. u9's
   `tests/invariants/style-as-data.test.ts` makes that exemption explicitly
   (`withoutFontFace()`) and asserts it removes face declarations and nothing
   else. Not fixed here: the assert lives in u1's file ([u9#c8]).

3. **Dead scaffold still on the tree. Owner: u0/u3 — delete.**
   `src/client/placeholder.ts` and `src/client/style.css` carry raw colour /
   font / px literals (`#4fc3f7`, `20px system-ui`, `background:#0d1117`) — real
   inv 8 violations by the letter. They are unreachable from the Vite entry
   (`src/main.ts → client/main.ts` retired them at u3), so they ship in no
   bundle. u9's inv 8 assert allowlists them and **proves** the exemption: the
   allowlist test fails the moment either file re-enters the player build graph.

4. **inv 2 cannot bind its real target yet.** LIVE FEED
   (`src/client/windows/live-feed.ts`) is still a u3 merge stub, so no
   `.fl-npc` / `.fl-symptom` node exists at runtime. The assert is written
   universally-quantified over what exists (source scan + selector-grounding +
   the rendered-DOM half in `e2e/a11y.spec.ts`) and tightens by itself at u5.
   The same holds for the membrane-op a11y assert: `[data-op]` controls arrive
   with u4/u6/u7, which are out of scope this run (C1, blocked on
   `src/shared/segment.ts`).

5. **`vitest.config.ts` runs `environment: 'node'`.** The "…and rendered DOM"
   clause of [u9#c1] and the selector-scoped clause of [u9#c2] therefore cannot
   live in `tests/invariants/`. Their browser halves are in `e2e/a11y.spec.ts`
   (`inv 1 · rendered DOM`, `inv 2 · rendered DOM`) — the only browser file this
   unit's globs allow. At u11 they may want their own spec files.

6. **Playwright's dev server races on a cold first run.** The first
   `npx playwright test e2e/a11y.spec.ts` after a fresh checkout intermittently
   yields `net::ERR_CONNECTION_REFUSED` on `http://localhost:5174/nhn-game-2026/`
   for the later workers; the immediate re-run is clean. `playwright.config.ts`
   `webServer.url` is polled but the Vite dev server appears to answer before its
   `/nhn-game-2026/` base is mounted. *Owner: u11 (harness) — consider polling a
   real asset under the base path rather than the base itself.*

## C. Scope notes ([u9#c6])

Every assert is unit-scoped or repo-scoped-with-a-**proven** allowlist:
`no-free-text` (allowlist empty), `no-digit-npc` (scoped by selector, every
scoped and excluded selector asserted to exist on disk), `style-as-data` (two
allowlist entries, each proven unreachable from the Vite entry),
`seam-integrity` (no allowlist), `a11y.spec.ts` (unit-scoped, forward-binding
— membrane-op asserts are universally quantified and pass at zero controls).
Nothing reads `demos/`, `proxy/`, `tools/`, `authoring/` or `data/`; the
repo-wide bind happens at u11.

## D. VERIFY findings (attempt 1) — reported, not fixed

Recorded during the rendered self-check. Every item is outside this unit's
`file_globs` (`tests/invariants/**`, `e2e/a11y.spec.ts`) and outside [u9#c8]'s
permission to write production code, so all are reported rather than fixed.
Shots: `.super-worktrees/20260803-213143/_shots/u9/{dev,build}-desk-1280x800.png`.

1. **A built `dist/` cannot boot the desk.** `npm run build` +
   `vite preview --base /nhn-game-2026/` paints the topbar and then throws
   `Unexpected token '<', "<!doctype "... is not valid JSON`; `#desktop` stays
   empty and none of `w-feed w-file w-store w-rep w-tally` mounts. The
   scenario-pack fetch falls through to the SPA index because the §3.7 pack-copy
   plugin does not exist yet — the same gap `playwright.config.ts` documents as
   its reason for driving the dev server. Consequence beyond u9: **any**
   production-build visual or smoke gate is impossible until §3.7 lands.
   *Owner: u11.*

2. **Invariant-vs-reference deviation, boot open-set (P0-A precedence).**
   `e2e/a11y.spec.ts:boot()` waits for all five windows to be visible, because
   that is the shell's actual default at this merge and [u9#c6] requires the
   assert to be green on the tree as it exists. The reference
   (`shell-desktop-1280x800.png`) shows TALLY **closed** at boot, with a toast
   (`RUN 03 진행 중 · 13:05 — 부검 창의 문장을 늘러 채굴하세요`) occupying that band.
   The spec-derived invariant was kept and the reference deviation recorded here.
   If u3 adopts the reference default, `WINDOW_IDS` in `boot()` must drop
   `w-tally` and a toast/live-region assert should replace it. *Owner: u3, then
   whoever re-baselines u9's boot helper.*

3. **Window bodies are empty at this merge.** All five panes render blank paper
   against a reference with full content (feed entries, REPORTS two-column
   `객관 로그`/`요원 보고서` with the mined-block thread, BLOCK STORE filter chips
   and cards, AGENT FILE `§0…§4` with the `배치 완료` stamp and redaction bars).
   Expected — u4–u8 are out of scope this run (C1). Named here only so the
   integrator does not read the blank desk as a regression.

4. **Smaller chrome deltas vs the reference** (all u3): corner tabs render
   `LF/RP/AF/BS/TL` where the reference has `LF-03/RP-02/AF-03`; the topbar CASE
   field reads `우는다리` where the reference has `우는다리 · 윤슬교`; the REPORTS
   subtitle reads `부검 — 시행 기록` where the reference has `부검 — 문장 채굴`.
