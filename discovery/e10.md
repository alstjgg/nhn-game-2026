# e10 — discovery

The acceptance unit. `tests/acceptance/` transcribes
`docs/contract-engine-composer.md` §8's ten criteria as one suite, and this file
is the written half of A20: the five integration gates are **commands**, not
vitest cases (`cd proxy && npm ci` mutates `proxy/node_modules`, and shelling
out would make the suite non-hermetic), so their results are recorded here and
`gates.test.ts` goes red if any of them is missing from this record.

## 1. The five integration gates — all green (2026-08-04)

| # | Gate | Command | Result |
|---|---|---|---|
| A14 | full suite | `npm test -- --exclude 'tests/scaffold/layout.test.ts'` | **green** — 56 files, 878 tests, 0 failed |
| A15 | isomorphism + type-drift + shared goldens | `npm run check` | **green** — `tsc -p tsconfig.core.json`, `tsc`, `typecheck:test`, `datapack:check`, `test:shared` (66 node:test checks, incl. D2's segmentation golden) |
| A16 | production build | `npm run build` | **green** — 7 modules, `dist/` in 438 ms |
| A17 | probe, offline | `npm run probe:selftest` | **green** — 44 checks |
| A18 | proxy from a clean install | `cd proxy && npm ci && npm test` | **green** — 3 files, 39 tests; D3 prompt byte-parity re-confirmed |
| A19 | frozen inputs | `git status --porcelain package.json package-lock.json` | **clean** — no dependency was added or moved |

D1 and D4 re-run at the end, against the fully integrated tree, not only inside
their own units:

| Gate | Command | Result |
|---|---|---|
| D1 | `npm test -- tests/engine/feed/id.golden.test.ts` | **green** — 9 tests, the id golden unchanged |
| D4 | `node tools/driver/drive-run.mjs --pack 우는다리 --provider fixture --determinism-check` | **green** — two passes of `우는다리-fixture-r1` byte-identical (19 beats, 108 timeline lines) |

`tests/scaffold/layout.test.ts` is excluded from A14 by the spec's own
assumption 1: it is the pre-existing `src/client/**` build census, red at this
base and against a frozen path (`discovery/e8.md` §7, `discovery/e9.md` §7). No
test was deleted or weakened to reach green.

## 2. K1 — §8-1's union needs a sixth term, for the player

§8-1 reads "every slot resolves to exactly one of `GateView ∪ BeatView ∪
RoundView ∪ ComposerDeps ∪ PROXY_OWNED_SLOTS`". Run literally over the live
views, that closure leaves **`BLOCKS`** unassigned in spirit: call contracts §6
assigns it to *the player* ("mined from the actual generated text of timeline
and reports"), and it reaches the composer through a construction dependency
(the block store) rather than through any view.

Papering over it by leaning on `COMPOSER_DEP_SLOTS` alone would have hidden the
fact that the composer does not *author* the slot, it only resolves ids the
player picked. So the closure's supplier map carries an explicit sixth term,
`PLAYER_SUPPLIED_SLOTS = ['BLOCKS']`
(`tests/acceptance/fixtures/closure.ts`), and `BLOCKS` being named by both
`PlayerSupplied` and `ComposerDeps` is deliberately **not** counted as a double
supply — the doubling rule is engine-side ∩ proxy-owned, not set ∩ set, because
the property §8-1 protects is "the client cannot rewrite the agent's character".

The same rule is what keeps `TEMPERAMENT` (in `GateView` *and* `RoundView`, by
requirement of §8-6) from reading as a double supply.

**Who resolves it:** whoever next edits contract-engine-composer §8 should add
the player to the union's wording. Nothing in `src/**` changes.

## 3. The stale §2.1 warning — `AGENT_UTTERANCE` **is** in §6 now

`docs/contract-engine-composer.md` §2.1 is a ⚠️ block titled "`AGENT_UTTERANCE`
is missing from call contracts §6", and §8's own §338 line repeats it as an open
item. It is stale: `docs/contract-calls.md` §6's "Supplier per slot" table now
carries the row

> `| 2 | AGENT_UTTERANCE | Call 1's utterance this beat, held by the engine. Empty string on a script beat |`

The parser in `fixtures/closure.ts` reads 15 slots out of that table,
`AGENT_UTTERANCE` among them, and the closure assigns it to `BeatView` with no
special case. The gap the warning describes has been closed in the document it
points at; the warning itself has not been retracted.

**Who resolves it:** the owner of `docs/contract-engine-composer.md` deletes
§2.1 and its §8 echo. `docs/**` is frozen for this unit, so nothing was edited.

## 4. What the suite drives, and why it is the real pack

Both rigs in `tests/acceptance/fixtures/rig.ts` run over the shipped 우는다리
datapack and the real merged `src/**` slice — schedule builder, state core, beat
driver, id allocator, feed builders, round assembler, composer, block store,
live driver, fixture provider — all imported, none re-implemented. A hand-built
fixture could satisfy every one of §8's criteria while leaving the shipped
scenario broken, and three criteria are only meaningful against real content:
§8-2 needs a beat whose effect moves `trust` (G1's buckets carry ±5…20), §8-8
needs a run that generates lines on every channel, §8-5 needs a real Call 1 →
Call 3 trace of a non-trivial `inner_note`.

Two seams are watched and neither is altered: the transport (requests and
response bodies, passed through untouched) and the `ViewEvent` stream.

`createScriptedEngine` (e7's `tests/driver/engine-fixtures/`) is reused as the
`EnginePort` rather than a second binder being written for this suite. It is
already parameterised by pack, and §8 is about what that port and its
collaborators produce. **Steer candidate**, restating `discovery/e9.md` §1: when
`src/engine/index.ts` `createEngine` lands, both it and
`tools/driver/run/bind.mjs`'s `createEnginePort` collapse into it, and this
fixture should re-point at the product factory.

## 5. `Driven` is narrower than the TEST note sketched

`.claude/super/units/e10/tests.md` sketches `Driven` with six fields
(`requests · responses · feed · events · journals · calls`). Only the first four
are read by any assertion, so only those four are built (YAGNI). `journals`
would additionally have required a state-core handle that `createScriptedEngine`
does not expose, i.e. a second engine binding written for a field nothing reads.

## 6. Scope overflow vs. `file_globs`

Declared globs were `tests/acceptance/**` and `discovery/e10.md`. Also written:
the `[e10]` entry in the root `DISCOVERY.md`, required by A20's last case. No
frozen path was touched: `src/client/**`, `proxy/**`, `docs/**`, `data/**`,
`tools/lib/**`, `tools/probe/**`, `package.json` and the three tsconfigs are all
unmodified (`git status` confirms).
