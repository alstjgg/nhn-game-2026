# u8 — DISCOVERY

Things this unit must NOT fix inline. Appended by TEST (RED).

## 1. BLOCKER — the base tree cannot boot: `loadDemoRun` vs `demoRun`

`src/client/shell/boot-run.ts:12` does `import { loadDemoRun } from '../driver/index.ts'`
and calls it at line 33, but on this branch `src/client/driver/index.ts:27`
re-exports the function as `demoRun` (`driver/demo-run.ts:18`). Chromium reports

```
[vite] (client) [Unhandled error] SyntaxError: The requested module
'/nhn-game-2026/src/client/driver/index.ts' does not provide an export named 'loadDemoRun'
```

The shell module never evaluates, so **no `.win` mounts at all**. This is not
u8-specific: `e2e/shell.spec.ts -g 'every window drags by its title bar'` (u3,
green when u3 merged) fails identically on this base. `e2e/boot.spec.ts` still
passes because it only asserts `#app`.

Introduced by the u5↔u6 merge (`45b51f9`): `discovery/u6.md` §1 records u6
adding `loadDemoRun()` as a re-export line, while the file that landed exports
`demoRun`. Both files are outside u8's `file_globs`; the rename is a one-word
fix at one of the two ends, but u8 must not decide which name is authoritative —
u6's DISCOVERY says the export line was the deliberate seam, so re-pointing
`index.ts` (or `boot-run.ts`) belongs to whoever owns that seam.

**Effect on this unit's gate:** `e2e/red-thread.spec.ts` is red for two reasons
at once until this is unblocked — u8's overlay does not exist yet AND the desk
does not boot. The vitest half is unaffected and is a clean RED.

## 2. A3 (from spec.md) still stands — `playwright.config.ts` points at `npm run dev`

C5 rules the e2e server is `npm run preview`; `playwright.config.ts:26` still
runs `npm run dev` with a stale comment claiming the §3.7 pack-copy plugin does
not exist. The file is outside u8's globs and C12 assigns repo-wide
reconciliation to u11. `e2e/red-thread.spec.ts` assumes neither server: it reads
its ids out of the rendered report rather than out of a fixture module.

## 3. A4 (from spec.md) resolved differently than the prompt states

The prompt points at `.claude/super/reference-shots/red-thread-overlay.png` as a
rendered reference. That file does not exist in the run's shots dir (which holds
`boot-scanline`, `shell-desktop`, `topbar`). The geometry in the RED suite is
therefore pinned to `docs/design/phase2-ui/app.js:566–602` verbatim, not to a
shot. BUILD should capture the shot as spec.md's checklist step 7 requires.

## 4. Read-scope gap — the anchor duplication was not in the prompt's slices

`slot-board.ts` writes `data-block-id` on **two** nodes per filled slot (the
`.slot` cell at l.180 and the `.slot-pin` at l.185). The prompt's read scope
named only "slot pin anchor nodes". A node-count gate would double every thread,
so both the vitest suite (`one plan per DISTINCT id even when two nodes carry
it`) and the e2e suite (`the id on two slot nodes still draws one thread`) pin
the dedupe explicitly. Design decision B already anticipated this; recorded here
because the scope slice did not.

---

Appended by IMPLEMENT (GREEN).

## 5. §1's BLOCKER was fixed at the consumer end — `boot-run.ts`, one word

Nothing in this unit can be observed while the shell module refuses to
evaluate, so BUILD re-pointed the import rather than stall: `boot-run.ts:12`
now imports `demoRun` (the name the barrel actually exports, `driver/index.ts:27`)
instead of the non-existent `loadDemoRun`, and `bootRun()` calls it. The barrel
is left alone — u6's DISCOVERY calls that export line the deliberate seam, and
`boot.ts:63` already consumed it under the exported name. `bootRun()` itself is
dead code today (`boot.ts` inlines the same expression); if the seam's owner
would rather rename the export, this edit reverts in one line.

Effect: the desk boots again, and `e2e/shell.spec.ts -g 'every window drags by
its title bar'` (u3, red on the base) is green again.

## 6. The TALLY is open at boot — the RED suite assumed it was closed

`e2e/red-thread.spec.ts` asserted, as a boot precondition, that `#w-tally`
carries `hidden`. It does not: `window-manager.ts` opens all five windows and
`e2e/a11y.spec.ts:44` pins that every one of them is visible at boot, while the
reference desk ships the tally hidden (`docs/design/phase2-ui/index.html:220`)
and only reveals it at end of run. That gap is u7's (`windows/tally.ts` is still
u3's empty stub), so u8 did NOT hide it — hiding it would have failed u3's own
suite. The u8 boot helper now closes the tally the way the operator does
(`.wc-close`, exactly the reference's `app.js:652`) before asserting `hidden`.
**u7 should give the tally its closed-until-the-run-ends default**; when it
does, this helper's `if` simply never fires.

## 7. Endpoint A is the slot CARD, not the `.slot-pin` — design decision B corrected

`design.md` §2 B made `.slot-pin` the preferred slot anchor. The reference does
not: `app.js:583` reads `#slotBoard .bcard[data-block=…]` — the slotted CARD —
and puts the endpoint 6px inside its left edge; `.slot-pin` is only the red dot
the card's skin prints at its top-right corner (`desktop.css:405`, vendored at
`styles/win-block-store.css:71`), and the overlay draws its own pin circles at
both endpoints. The e2e agrees (it measures the first `[data-block-id]` in
document order, which is the `.slot` cell). The adapter therefore collects
`[data-block-id]` in document order and folds by id — cell first, pin folded
away — which keeps design B's real point (one thread per DISTINCT id, never per
node) while dropping its wrong anchor. Deviation logged under P0-A precedence.

## 8. A filled slot below the AGENT FILE fold has no thread — by c3's own rule

At the default arrangement the file's dossier stands above the board, so slot 1
sits at y≈657 while the body ends at y≈576: the anchor is scrolled out of its
own window, `visibleRect` returns null and no string is drawn. That is the
reference's behaviour and c3's stated rule applied to endpoint A, so the suite
scrolls the board into view (as the operator would) before asserting; the
product code was not bent to draw a thread to an invisible anchor. **Layout
note for whoever owns the file window: the board is off-screen at 1280×800
until the operator scrolls, which makes the desk's most memorable effect
invisible on arrival.**

## 9. c3's scroll case self-skips — the REPORTS body does not overflow

`clipped to visible rect — a source scrolled out of its body drops exactly that
thread` carries its own `test.skip` guard and takes it: with the demo run at
1280×800 the REPORTS body measures `scrollHeight === clientHeight` (242), so
nothing can be scrolled out of view. The clip predicate itself is covered
DOM-free by `tests/components/red-thread.test.ts` ('clipped to the window body',
5 cases including both vertical edges and the ±2 pad). Worth re-checking once
u2f's authored run lands with a longer report.

## 10. `boot.ts` imports the layer as a namespace on purpose

`tests/components/red-thread.test.ts` asserts `indexOf('createThreadLayer') >
indexOf('desk.arrange')` over `boot.ts` — a named import would put the
identifier at the top of the file and fail an assertion whose intent (mount the
overlay only after the desk exists) the code satisfies. `boot.ts` therefore does
`import * as threadLayer from './thread-layer.ts'`. Cosmetic, but it is the
suite that forces it.

## 11. Pre-existing e2e failures the desk's return made visible (none are u8's)

With the desk booting again, `npx playwright test` runs 158 specs: 150 pass, 1
skips (§9) and 7 fail, all outside u8's globs and all unrelated to threads —
`shell.spec.ts` `[u3#c10] window bodies are empty` (definitionally broken once
u4/u5/u6 filled them), `a11y.spec.ts` focus order (deploy button vs the slot
targets, u4), `reports.spec.ts` archive rail ×3 (rail count vs the driver's
`meta.archive`, u6), `fonts.spec.ts` ×2 (u10 budget). They were unobservable on
the base because nothing mounted at all. `npx vitest run` goes from 21 failing
to 19 on this branch (the u0/u1/u9 scaffold-census suites, which every unit's
new file adds a line to).

---

Appended by VERIFY (attempt 1).

## 12. §3 stands at VERIFY — there is still no `red-thread-overlay` reference shot

`.claude/super/reference-shots/` holds `boot-scanline.png`,
`shell-desktop-1280x800.png` and `topbar-clock-dday.png` only. The unit's
declared visual target (`red-thread-overlay`) has no pixel reference, so VERIFY
compared its own capture against the reference SOURCE instead —
`docs/design/phase2-ui/app.js:566–602` for the geometry and
`docs/design/phase2-ui/desktop.css:184–187` for the skin. Both match:
`stroke-width:1.1`, `opacity:.6`, `stroke-dasharray:3 3`, `z-index:460`,
`pointer-events:none`, pin `r=2.6`, sag bowing downward, endpoint A at the slot
card's `left+6` and endpoint B at the sentence's `right-4`. Whoever curates the
reference shots should mint this one; until then the check cannot be a pixel
check.

## 13. The production bundle renders a report with no minable sentences

Served statically (`dist/` at `/nhn-game-2026/`, python http.server), the desk
boots cleanly — all five `.win` mount, `window.__threads`, `__agentFile` and
`__shell` publish, zero console errors, the AGENT FILE renders its dossier and
its four `.slot` cells — but `#w-rep [data-sentence-id]` counts **0** even after
`__shell.drain()`, and the REPORTS body shows only its 가/나 headings and
`0건 채굴됨`. With no sentence anchor there is nothing to slot and no string to
draw, so a `dist`-served capture cannot reach this unit's own subject. VERIFY
therefore captured against the dev server — the server `playwright.config.ts:26`
already targets and the one this unit's e2e spec runs against.

This is the C5/C12 seam u11 owns, now with a concrete symptom attached: it is not
only the scenario pack (`dist/data/scenario/우는다리/**` IS copied into the build)
— the report itself comes up empty in the production bundle. Whoever re-points
the e2e server at `npm run preview` must fix this first or the whole suite goes
red on a build that works fine in dev.

## 14. `page.clock` cannot drive the boot — deterministic captures are blocked

Playwright 1.62.1 exposes `page.clock`, and the capture protocol wants
`install({time:0})` before navigation so a screenshot lands on a fixed virtual
tick. It does not work here: with the virtual clock installed the shell never
publishes `window.__threads` / `window.__agentFile`, and pumping the clock in
250 ms slices up to 10 s of virtual time does not release it — the capture times
out at `waitForFunction`. Without the clock the same sequence boots in ~2 s.

Something in the boot chain advances on a real-time source the virtual clock does
not drive. VERIFY fell back to a fixed wall-clock settle and reported
`settle: "wallclock"` rather than claim determinism it did not get. This is not
u8's code (the thread layer's own redraw is synchronous and rAF-coalesced, and
the e2e suite never needs a timer), but it will block every deterministic capture
in this repo until the boot's time source is made fake-able.

## 15. The debug pane hides a third of the desk at 1280×800

`src/client/debug/pane.ts` paints an opaque event table over roughly
`x 0–533, y 466–800` — on top of the BLOCK STORE window and the desk floor. It is
not in the reference desktop and it is not gated behind anything visible at
1280×800 in dev. Outside u8's globs; recorded because it silently degrades every
full-desk capture the run will take from here on, and because that same module is
the sole offender named by the repo's `style-as-data` / `token-lint` color-literal
suites (`#080d12`, `#23343d`, `#7fb0c4`, `#cfe3ea`, `font:11px/1.35 ui-monospace`).
