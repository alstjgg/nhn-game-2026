# u10 — DISCOVERY

Things this unit must NOT fix inline. Appended by IMPLEMENT (attempt 1).

## 1. Invariant-vs-reference deviation applied (P0-A precedence)

`docs/design/phase2-ui/index.html` (head) loads the three families from
`fonts.googleapis.com` / `fonts.gstatic.com`. spec-client §3 inv 10 forbids any
third-party request, so u10 ported the **compliant equivalent** instead of the
reference's own markup: the same eleven family/weight/style faces, the same
per-`unicode-range` slicing, vendored under `public/assets/fonts/` and declared
by `src/client/styles/fonts.css`. The reference file itself is untouched.
`docs/design/phase2-ui/README.md` lines 90–93 asks for exactly this.

## 2. Upstream slices are **not** disjoint — the partition is ours

`tests/assets/unicode-range-coverage.test.ts` requires the slices of one Korean
family/weight to be non-overlapping. Google's own sheet is not: each Korean
weight ends with a "most common glyphs" slice (ASCII + ~30 high-frequency
syllables) and a `latin` slice that both re-declare codepoints the numbered
block slices already carry; the CSS cascade resolves each codepoint to the
*last* face that claims it.

`tools/fonts/vendor-google-webfonts.mjs` therefore bakes that cascade into the
declarations — walking each family/weight backwards, a face keeps only what no
later face claimed. Coverage is byte-for-byte the upstream coverage; the
declarations are a true partition. Worth knowing at integration: this is a
deliberate divergence from upstream's literal `unicode-range` strings, and
re-running the vendor script is the only supported way to regenerate them.

## 3. Seam friction — nothing mounts `styles/index.css` yet, but `dist/` must carry the sheet

`tests/assets/no-third-party-url.test.ts` c5(b/c) requires the **built** output
to contain a stylesheet with `@font-face` + `unicode-range`. Nothing imports
`src/client/styles/index.css` today (the boot root still renders u0's
placeholder), so no skin CSS reaches the bundle at all.

The two obvious fixes both break `e2e/fonts.spec.ts` "load budget": that spec
injects its **own** probe copy of every `@font-face` via `addStyleTag`, so if
the document already declares the same faces, Chrome loads *both* FontFace sets
and every slice is fetched twice — measured 26 slices / ~490 KB against budgets
of 24 / 300 KB. Linking the sheet from `index.html`, importing it from
`main.ts`, or importing `index.css` from `main.ts` all trip this.

u10 therefore ships the sheet the way a self-hosted font pack normally ships
it: `public/assets/fonts/fonts.css`, generated by the same script, addressing
its slices **relatively** so it is correct under any `base` and needs no build
step. `src/client/styles/fonts.css` stays the bundler-side sheet (root-relative
urls, which Vite rebases) and `styles/index.css` imports it, ready for whoever
mounts the skin.

**For u1 / u11 / the integrator:** once the skin is actually mounted, the
document *will* declare these faces and the "load budget" probe will
double-count. That spec needs to switch from `addStyleTag` to measuring the
page's own faces at that point — u10 must not pre-empt it.

## 4. Cross-unit guard collisions (expected; not fixable inside u10)

u1's and u0's suites assert that *u10 has not happened yet*. With u10 in the
tree, 8 of their assertions fail. All of them are "the absence of u10" checks,
none is a real defect:

| suite | assertion | why it now fails |
| --- | --- | --- |
| `tests/styles/index-order.test.ts` | `[u1#c4]` (b) every import after the fourth is a per-window skin | `./fonts.css` is a tenth import |
| `tests/styles/index-order.test.ts` | `[u1#c4]` (d) exactly the nine u1 sheets are imported | same |
| `tests/styles/index-order.test.ts` | `[u1#c4]` fonts.css slot (a) u1 does not import fonts.css | u10 c8 *requires* that import |
| `tests/styles/index-order.test.ts` | `[u1#c4]` fonts.css slot (b) `styles/fonts.css` does not exist | u10 c1 *requires* the file |
| `tests/styles/hard-constraints.test.ts` | `[u1#c7]` (a) no `@font-face` in any sheet in `styles/` | it scans the whole directory, u10's sheet included |
| `tests/styles/hard-constraints.test.ts` | `[u1#c7]` (b) no font-file reference in `styles/` | same |
| `tests/styles/hard-constraints.test.ts` | `[u1#c7]` (c) `public/assets/fonts/` does not exist | u10 c1 *requires* it |
| `tests/styles/token-lint.test.ts` | `[u1#c1]` (a) `font-family` outside tokens.css must be `var()`-driven | a `@font-face` descriptor cannot be a `var()` |

u10 placed its import **last** in `index.css` specifically so `[u1#c4]` (a)
("the first four imports are tokens, base, shell, paper") keeps passing; that
was the only one of the nine that could be saved.

The reconciliation belongs to whoever owns those suites: their scope needs to
exclude `styles/fonts.css` / `public/assets/fonts/` rather than assert its
absence. u10 did not edit another unit's tests.

Pre-existing and unrelated: `tests/scaffold/layout.test.ts` `[u0#c8]` (2
assertions) was already red before u10 — u1's stylesheets violate u0's
"empty modules only" census.

## 5. Read-scope note

`.claude/super/units/u10.md` (listed first in the read scope) does not exist in
the worktree; only `.claude/super/units/u10/tests.md` does. The acceptance
criteria in the task prompt plus that test list were used as the contract.

---

## 6. VERIFY (attempt 1) — measured seam findings

Appended by the VERIFY agent. Gate: 6/6 acceptance commands pass; frozen-path
and reference-path guards clean; the two RED-time baselines were re-derived from
`super/20260803-213143` and match byte-for-byte (they were not regenerated to go
green).

### 6.1 The built document links none of the shipped faces

Probed on `vite preview` over the real `dist/` (1280×800, virtual clock at
t=1000 ms):

```
document.styleSheets → ["…/assets/index-CkUiP7lc.css", "(inline)"]
document.fonts.size  → 0
getComputedStyle(body).fontFamily → "system-ui, -apple-system, sans-serif"
dist/index.html      → references only assets/index-*.js and assets/index-*.css
```

`dist/assets/fonts/fonts.css` (249 KB, 492 `@font-face`) is emitted but nothing
links it, and `src/client/styles/index.css` is imported by no entry module, so
the skin sheet never enters the bundle (`dist/assets/index-*.css` is 0.38 KB of
u0 placeholder styling). `[u10#c5]`'s "the built CSS carries `@font-face`" is
therefore satisfied by the static copy in `dist/`, not by a sheet the document
actually applies — consistent with §3, but worth stating as a measurement.

**Action belongs to the unit that mounts the shell**, not to u10: import
`src/client/styles/index.css` from the client entry, or add
`<link rel="stylesheet" href="assets/fonts/fonts.css">` to `index.html`.
Until then the 8.9 MiB pack is dead weight in the build and no rendered surface
exercises the three families. When that import lands, check the double-declare
risk §3 describes: the document would then carry the pack sheet *and* the
bundled skin sheet if both are linked — link exactly one.

### 6.2 `DISCOVERY.md` was written directly by this unit

Commit `126c192` appends 15 lines to the repo-root `DISCOVERY.md` in addition to
`discovery/u10.md`. The per-unit file is the one the integrator consolidates; a
shared append-file conflicts at nearly every merge barrier. Integrator: prefer
`discovery/u10.md` and drop u10's root-`DISCOVERY.md` hunk if it collides.

### 6.3 Cross-unit oracle collisions — confirmed, and one correction

§4's table reproduces exactly (8 assertions across `tests/styles/**`). Confirmed
independently that `tests/scaffold/layout.test.ts` `[u0#c8]` (2 assertions) is
red at the base commit already — u1's stylesheets exist there — so the repo-wide
count is 10 failures, of which none is caused by u10's own files. All of them
live outside u10's `file_globs`; the reconciliation is a scope fix in the u0/u1
suites (exclude `styles/fonts.css` and `public/assets/fonts/` rather than assert
their absence).
