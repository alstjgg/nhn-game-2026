# discovery — u0

- **Harness-vs-contract conflict (resolved in favour of the contract).** The
  agent instructions say to append to `discovery/<unit-id>.md` and never write
  `DISCOVERY.md` directly, but u0's acceptance criterion **c6** (and
  `tests/scaffold/discovery.test.ts`) requires the repo-root `DISCOVERY.md` to
  exist with the three run sections seeded. u0 therefore **creates**
  `DISCOVERY.md` as the run-wide append target — that file *is* this unit's
  deliverable. All of u0's own findings are written there, under
  `## Spec gaps` / `## Seam friction` / `## Reference ambiguities`; nothing is
  duplicated here, so the integrator should not append this file's contents
  into `DISCOVERY.md`. Later units append to `DISCOVERY.md` via their own
  `discovery/<unit>.md` files as usual.
- **Read-scope miss.** The sliced read-scope for u0 did not include
  `docs/plan-client-build.md` §1–§2 or `docs/spec-physical-architecture.md`
  §1.1, both of which were needed to write the c6 discovery entries accurately
  (the memory-only/`sessionStorage` contradiction and the "dev deps only"
  rule). Opened those sections only.

## verify (attempt 1) — findings raised after the implement pass

The "do not consolidate this file" note above was written by IMPLEMENT and covers
**only** the entries above it, which are already in `DISCOVERY.md`. The entries in
this section are **new** and are *not* in `DISCOVERY.md` — the integrator should
consolidate these under `## Seam friction`.

- **[u0] Retiring `placeholder.ts` is a cross-unit test edit, not just a source
  edit.** `src/client/main.ts` still boots `mountPlaceholder()`, so the built site
  renders the deploy-smoke canvas rather than the shell — correct for u0 (its own
  `[u0#c8] empty-modules-only census` in `tests/scaffold/layout.test.ts` forbids
  any other file under `src/client/`). But that test hard-codes
  `PREEXISTING = {'src/client/placeholder.ts', 'src/client/style.css'}` and allows
  nothing beyond `.gitkeep` + `main.ts`, so **the unit that mounts the real shell
  (u1 per the `main.ts` comment) must edit `tests/scaffold/layout.test.ts` as part
  of its own change** or u0's census will fail the moment a shell module appears.
  If `tests/scaffold/**` is not in that unit's `file_globs`, the seam is blocked —
  flagging it now rather than letting it surface as a mid-run gate failure.
- **[u0] `index.html` `<title>` is still "NHN Game 2026 — placeholder".** The
  reference shell is a NDSP-2 / 우는다리 case surface. The title is a u0-owned file
  (`index.html`) but a downstream concern; whoever mounts the shell owns the rename.
- **[u0] Reference shots are time-bearing, so downstream visual verify needs a
  driven clock, not just a settled one.**
  `.claude/super/reference-shots/boot-scanline.png` shows live values (`SIM 13:18`,
  `D-DAY −07`, `RUN 03 / 10`). u0's capture used
  `page.clock.install({time:0})` before navigation + `clock.runFor(1200)`, which was
  enough for the rAF-driven placeholder (at tick 0 the canvas is blank). Downstream
  units must additionally reach the *driver* state behind those values — a fixed
  virtual tick alone will not reproduce them unless the driver is seeded.

