# The Lambda runtime is nodejs24.x and package.json pins `engines.node >= 24`.
# Without this, npm downgrades that pin to an EBADENGINE warning that scrolls
# past — install succeeds on node 22 and the failure surfaces later, as a test
# or a bundle that behaves differently from the deployed one. Fail at install.
engine-strict=true
